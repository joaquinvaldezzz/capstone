/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    autoprefixer: {},
    'postcss-import': {},
    'postcss-sort-media-queries': {},
    tailwindcss: {},
  },
}

export default config
